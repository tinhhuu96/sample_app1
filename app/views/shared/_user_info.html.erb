<%= link_to gravatar_for(current_user, size: Settings.micropost.size_gravata), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to "view my profile", current_user %></span>
<span><%= pluralize(current_user.microposts.count, t(".cn1")) %></span>
